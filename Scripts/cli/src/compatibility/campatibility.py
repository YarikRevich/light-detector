STM_ELECTRONICS = "STM"


def is_manufacturer_allowed(src: str) -> bool:
    """Checks if the given manufacturer is among of allowed ones."""

    return True


def is_device_allowed(src: str) -> bool:
    """Checks if the given device name is among of allowed ones."""

    return True
