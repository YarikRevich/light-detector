#ifndef LIGHT_DETECTOR_STATE_H
#define LIGHT_DETECTOR_STATE_H

/**
 * Represents device state handler.
 */
class State {
public:
    /**
    * Handles incoming action to
    */
    static void handle_check();
};

#endif //LIGHT_DETECTOR_STATE_H
